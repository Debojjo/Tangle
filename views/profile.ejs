<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-800 text-white p-10">
        <h1 class="text-center text-4xl mb-5 text-orange-500">Tangle🍂</h1>
        <a class="absolute top-0 right-0 mt-2 mr-2 bg-red-500 p-2 rounded-md text-sm inline-block"
            href="/logout">LogOut</a>
        <div class="flex gap-2">
            <div class="w-10 h-10 rounded-lg">
                <img class="w-full h-full object-cover overflow-hidden" src="/images/upload/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl mb-5"><span class=" text-zinc-400">Hello, </span>
                <%= user.name %>🖐
            </h3>
        </div>
        <h5 class="mt-3 mb-4 text-yellow-500">Create a new post</h5>
        <form method="post" action="/post">
            <textarea placeholder="Current vibe.. "
                class="w-1/3 p-2 resize-none bg-transparent border-2 border-zinc-700 rounded-md outline-none"
                name="content"></textarea>
            <input class="text-black p-1 w-1/6 mt-1 bg-yellow-500 block rounded-md text-sm" type="submit" value="Post">
        </form>
        <div class="posts mt-20 ">
            <h3 class="text-yellow-400 mb-2">Your posts</h3>
            <div class="postconatiners flex gap-4">
                <% user.posts.forEach(function(post){%>
                    <div class="post w-1/3 p-5  mb-5 border-2 border-zinc-600 bg-zinc-700 rounded-md">
                        <h4 class="text-blue-500">
                            <%= user.username %>
                        </h4>
                        <p class="text-zinc-200 text-sm tracking-tight">
                            <%= post.content %>
                        </p>

                        <div class="btns flex gap-5 mt-5">
                            <a href="/like/<%= post._id %>">👍<small class="mt-1 inline-block ">
                                    <%= post.likes.length %>
                                </small></a>
                            <a href="/unlike/<%= post._id %>">👎<small class="mt-1 inline-block ">
                                    <%= post.unlikes.length %>
                                </small></a>
                            <a class="text-sm text-pink-500" href="/edit/<%= post._id  %>">Edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>